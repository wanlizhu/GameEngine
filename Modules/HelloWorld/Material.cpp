#include "Material.h"
#include "IResourceTable.h"

void Material::initWithMaterialDesc(const MaterialDesc& desc,
                                    IDevice* device)
{}

void Material::apply(IDevice* device,
                     IResourceTable* resourceTable,
                     Camera* camera)
{
    
}
